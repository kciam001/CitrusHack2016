{
  "manifest_version": 2,

  "name": "Open Classes Notifier(tbd)",
  "description": "This extension checks a certain class and notifies the user if the class is still open.",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab",
    "https://ajax.googleapis.com/"
  ]
}